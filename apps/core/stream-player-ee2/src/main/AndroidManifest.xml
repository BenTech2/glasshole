<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- URL receiver - launches PlayerActivity with EXTRA_URL -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="com.google.android.glass.category.DIRECTORY" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.glasshole.streamplayer.ACTION_PLAY_URL" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Unified stream player — Twitch HLS, YouTube live/VOD, direct HLS -->
        <activity
            android:name=".PlayerActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:theme="@style/AppTheme.Fullscreen" />

        <!-- WebView fallback for arbitrary URLs (TikTok, Facebook, Twitter, …)
             that we can't extract natively. Loads the page with HTML5 video. -->
        <activity
            android:name=".WebViewPlayerActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:hardwareAccelerated="true"
            android:theme="@style/AppTheme.Fullscreen" />

        <!-- GlassHole plugin service — receives PLAY_URL from phone and hands
             the URL to our own MainActivity. -->
        <service
            android:name=".StreamPluginService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.glasshole.plugin.GLASS_PLUGIN" />
            </intent-filter>
            <meta-data
                android:name="com.glasshole.plugin.ID"
                android:value="stream" />
        </service>

    </application>
</manifest>

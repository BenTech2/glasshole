<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"
    android:focusable="false"
    android:focusableInTouchMode="false"
    android:descendantFocusability="blocksDescendants">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/notifPager"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:orientation="horizontal"
            android:focusable="false"
            android:focusableInTouchMode="false"
            android:descendantFocusability="blocksDescendants" />

        <com.glasshole.glassxe.home.DotsView
            android:id="@+id/notifDots"
            android:layout_width="match_parent"
            android:layout_height="10dp"
            android:layout_marginBottom="8dp" />

    </LinearLayout>

    <!-- Per-notification options overlay. Translucent so the underlying
         card remains faintly visible — matches Glass's OK-menu feel. -->
    <LinearLayout
        android:id="@+id/notifOptions"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#C0000000"
        android:orientation="vertical"
        android:gravity="center"
        android:visibility="gone">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#888888"
            android:textSize="15sp"
            android:fontFamily="sans-serif-light"
            android:letterSpacing="0.05"
            android:text="tap to confirm · swipe to choose · swipe down to cancel" />

        <!-- Options are built dynamically from the current notification's
             actions + a synthetic Dismiss; see NotificationDrawerActivity. -->
        <LinearLayout
            android:id="@+id/optionsRow"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="28dp"
            android:orientation="horizontal"
            android:gravity="center_vertical" />

    </LinearLayout>

</FrameLayout>

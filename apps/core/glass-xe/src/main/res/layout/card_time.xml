<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <!-- Stats overlay — CPU / RAM / thermal one-liner. Hidden by
         default; phone-side Debug settings toggles it on via
         SET_SHOW_STATS_OVERLAY. Pinned top-center between the
         status icons (top-start) and battery chip (top-end). -->
    <TextView
        android:id="@+id/statsOverlay"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|center_horizontal"
        android:layout_marginTop="14dp"
        android:textSize="15sp"
        android:textColor="#FFFFFFFF"
        android:textStyle="bold"
        android:fontFamily="sans-serif-medium"
        android:shadowColor="#80000000"
        android:shadowDx="0"
        android:shadowDy="1"
        android:shadowRadius="3"
        android:visibility="gone" />

    <!-- Top status icons: phone (BT) and Wi-Fi. Each is hidden when
         the corresponding link is down — we intentionally show nothing
         rather than a "disconnected" placeholder. Default position is
         top-start; CardAdapter mirrors layout_gravity + margins to
         top-end when KEY_SWAP_TOP_BAR is on. -->
    <LinearLayout
        android:id="@+id/statusIconsRow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_marginTop="12dp"
        android:layout_marginStart="16dp"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageView
            android:id="@+id/phoneStatusIcon"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginEnd="10dp"
            android:src="@drawable/ic_phone_link"
            android:visibility="gone"
            android:contentDescription="Phone connected" />

        <ImageView
            android:id="@+id/wifiStatusIcon"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_wifi"
            android:visibility="gone"
            android:contentDescription="Wi-Fi connected" />

    </LinearLayout>

    <!-- Top battery indicator. Default position top-end; CardAdapter
         mirrors to top-start when KEY_SWAP_TOP_BAR is on. The numeric
         percent text is hidden when KEY_SHOW_BATTERY_PERCENT is off. -->
    <LinearLayout
        android:id="@+id/batteryRow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_marginTop="12dp"
        android:layout_marginEnd="16dp"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <com.glasshole.glassxe.home.BatteryIndicatorView
            android:id="@+id/batteryIcon"
            android:layout_width="36dp"
            android:layout_height="18dp"
            android:layout_marginEnd="8dp"
            android:contentDescription="Battery" />

        <TextView
            android:id="@+id/batteryText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#B0BEC5"
            android:textSize="20sp"
            android:fontFamily="sans-serif-medium"
            android:text="" />
    </LinearLayout>

    <!-- Centered clock stack -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center">

        <TextView
            android:id="@+id/timeText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#FFFFFF"
            android:textSize="72sp"
            android:textStyle="bold"
            android:includeFontPadding="false"
            android:letterSpacing="-0.02"
            android:text="—:—" />

        <TextView
            android:id="@+id/ampmText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#FFC107"
            android:textSize="16sp"
            android:textStyle="bold"
            android:letterSpacing="0.1"
            android:text="" />

        <TextView
            android:id="@+id/dateText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:textColor="#CCCCCC"
            android:textSize="18sp"
            android:text="" />

    </LinearLayout>

    <!-- Bottom-center app-drawer affordance — tap the card to open. -->
    <ImageView
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="14dp"
        android:src="@drawable/ic_app_drawer_grid"
        android:alpha="0.7"
        android:contentDescription="Open app drawer" />

    <!-- Bottom-start weather block. Populated by CardAdapter.bindTime
         from the cached payload (phone fetches Open-Meteo and ships
         the latest over BT). Bigger than the v1 chip and gains an
         AQI row colored by US category. Hidden when there's no fresh
         data. -->
    <LinearLayout
        android:id="@+id/weatherChip"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|start"
        android:layout_marginStart="16dp"
        android:layout_marginBottom="14dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:visibility="gone">

        <ImageView
            android:id="@+id/weatherIcon"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_marginEnd="10dp"
            android:src="@drawable/ic_weather_clear"
            android:contentDescription="Weather" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView
                android:id="@+id/weatherTemp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#FFFFFF"
                android:textSize="28sp"
                android:fontFamily="sans-serif-medium"
                android:includeFontPadding="false"
                android:text="" />

            <TextView
                android:id="@+id/weatherHiLo"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#B0BEC5"
                android:textSize="12sp"
                android:fontFamily="sans-serif"
                android:includeFontPadding="false"
                android:text="" />

            <TextView
                android:id="@+id/weatherAqi"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#A5D6A7"
                android:textSize="12sp"
                android:fontFamily="sans-serif-medium"
                android:includeFontPadding="false"
                android:visibility="gone"
                android:text="" />

        </LinearLayout>
    </LinearLayout>

</FrameLayout>

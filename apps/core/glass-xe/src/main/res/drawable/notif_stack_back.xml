<?xml version="1.0" encoding="utf-8"?>
<!-- Sliver poking out above the front notification card, to imply
     there's another notification stacked behind it. Rounded top corners
     only — the bottom is hidden by the front card so we keep it square
     and let the front card's edge be the natural cut-off. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#3A3A3A" />
    <stroke
        android:width="1dp"
        android:color="#80FFFFFF" />
    <corners
        android:topLeftRadius="6dp"
        android:topRightRadius="6dp"
        android:bottomLeftRadius="0dp"
        android:bottomRightRadius="0dp" />
</shape>

<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- WiFi-LAN file transfer: glass spins up a one-shot HTTP server
         on the local network and the phone fetches the requested file
         from there. Beats base64-over-BT-RFCOMM by ~100×. INTERNET +
         ACCESS_WIFI_STATE so we can detect the WiFi IP and bind a
         ServerSocket to it. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:label="Photos Sync"
        android:requestLegacyExternalStorage="true">

        <service
            android:name=".GalleryGlassPluginService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.glasshole.plugin.GLASS_PLUGIN" />
            </intent-filter>
            <meta-data android:name="com.glasshole.plugin.ID" android:value="gallery" />
        </service>

        <!-- Launcher-visible tile that just prompts for storage access. The
             glass service itself has no UI, so this is how users grant the
             runtime permissions without adb. -->
        <activity
            android:name=".PermissionActivity"
            android:label="Gallery Access"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>

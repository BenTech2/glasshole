<?xml version="1.0" encoding="utf-8"?>
<!-- About card: leftmost on the main carousel. Plain-text dump of
     app version + build counter. Read-only — tap is a no-op,
     swipe-down sleeps like any non-Exit card. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#FFFFFF"
            android:textSize="40sp"
            android:fontFamily="sans-serif-light"
            android:includeFontPadding="false"
            android:text="GlassHole" />

        <TextView
            android:id="@+id/aboutVersionLine"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:textColor="#90CAF9"
            android:textSize="18sp"
            android:fontFamily="sans-serif"
            android:text="v0.0.0 · build 0" />

        <TextView
            android:id="@+id/aboutNetwork"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="14dp"
            android:textColor="#90A4AE"
            android:textSize="12sp"
            android:fontFamily="sans-serif"
            android:gravity="center"
            android:lineSpacingExtra="1dp"
            android:text="" />

    </LinearLayout>

</FrameLayout>

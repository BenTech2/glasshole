<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:label="GlassHole Gallery"
        android:requestLegacyExternalStorage="true">

        <service
            android:name=".GalleryGlassPluginService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.glasshole.plugin.GLASS_PLUGIN" />
            </intent-filter>
            <meta-data android:name="com.glasshole.plugin.ID" android:value="gallery" />
        </service>

        <!-- Launcher-visible tile that just prompts for storage access. The
             glass service itself has no UI, so this is how users grant the
             runtime permissions without adb. -->
        <activity
            android:name=".PermissionActivity"
            android:label="Gallery Access"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
